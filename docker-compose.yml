#version: '3.7'
#services:
#  mongodb_container:
#    image: mongo:latest
#    environment:
#      MONGO_INITDB_ROOT_USERNAME: root
#      MONGO_INITDB_ROOT_PASSWORD: mongopw
#    ports:
#      - 27017:27017
#    volumes:
#      - mongodb_data_container:/data/db
#
#volumes:
#  mongodb_data_container:

version: '3.7'

services:
  database:
    image: 'postgres:latest'
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: delivery_database_1
    volumes:
#      - ./postgres-data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/in
        it.sql
    networks:
      - mvp-network

#  delivery-mvp:
#    image: delivery-mvp:latest
#    container_name: delivery
#    build:
#      context: ./
#      dockerfile: Dockerfile
#    depends_on:
#      - database
#    ports:
#      - "8099:8099"
#    networks:
#      - mvp-network

networks:
  mvp-network:
    driver: bridge